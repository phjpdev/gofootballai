# Public API URL (baked into client bundle at build time)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Server-side backend URL for Next.js API routes (production: http://127.0.0.1:4000)
# API_URL=http://127.0.0.1:4000
